#include <stdio.h>
int main(){
  printf("Enter the first number: ");
  scanf("%d" &a);
  printf("Enter the second number: ");
  scanf("%d" &b);
  product = a*b;
  printf("The product is: %d" product)
}
