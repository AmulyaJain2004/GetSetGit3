#include <stdio.h>

void main() {
  float a,b;
  printf("Operands:");
    scanf("%f %f", &a, &b);
  
  // write your own code 
  
  switch op {
    case "add": 
             a + b;
    case "mod":
    a%b; // gives the remainder 
    }
}
